<%= form_for :comment, url: {action: "create_comment", params: {parent_id: parent_id}, html: {id: "comment_form"}} do |form| %>
  <% if comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_area :content, required: true%>
  </div>

  <div>
    <%= form.label :user_name, style: "display: block" %>
    <%= form.text_field :user_name, required: true %>
  </div>
  <%= form.hidden_field :parent_id, value: parent_id %>
  <div>
    <%= form.submit "Add comment" %>
  </div>
<% end %>